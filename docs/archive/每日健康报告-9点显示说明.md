# 每日健康报告 - 9点显示昨天数据

## 📋 功能说明

每日健康报告已优化为**每天9点后首次启动时显示昨天的健康数据**。

## 🕐 显示规则

### 时间条件
- **触发时间**: 每天上午9:00或之后
- **显示频率**: 每天只显示一次
- **启动延迟**: VS Code 启动后延迟5秒显示

### 数据条件
- **数据来源**: 显示昨天（前一天）的健康数据
- **数据要求**: 昨天必须有健康活动记录
  - 起身次数 > 0 或
  - 喝水次数 > 0

### 显示逻辑
```
启动 VS Code
    ↓
延迟 5 秒
    ↓
检查当前时间是否 >= 9:00
    ↓ 是
检查今天是否已显示过报告
    ↓ 否
获取昨天的健康数据
    ↓
检查昨天是否有活动记录
    ↓ 是
显示每日健康报告
```

## 💡 使用场景

### 场景 1: 早上9点前启动
```
时间: 8:30 AM
操作: 启动 VS Code
结果: ❌ 不显示报告（未到9点）
```

### 场景 2: 早上9点后首次启动
```
时间: 9:15 AM
操作: 启动 VS Code
结果: ✅ 显示昨天的健康报告
```

### 场景 3: 早上9点后第二次启动
```
时间: 10:00 AM
操作: 再次启动 VS Code
结果: ❌ 不显示报告（今天已显示过）
```

### 场景 4: 昨天没有数据
```
时间: 9:30 AM
昨天数据: 起身0次，喝水0次
结果: ❌ 不显示报告（昨天无活动）
```

## 🎯 设计理由

### 为什么是9点？
- ✅ 大多数人9点左右开始工作
- ✅ 避免过早打扰用户
- ✅ 给用户足够时间查看和反思

### 为什么显示昨天的数据？
- ✅ 昨天的数据已完整，更有参考价值
- ✅ 可以在新的一天开始时回顾昨天
- ✅ 根据昨天的表现调整今天的计划

### 为什么延迟5秒？
- ✅ 避免启动时立即弹窗打扰
- ✅ 让 VS Code 完全加载完成
- ✅ 给用户缓冲时间

## 📊 报告内容

### 显示信息
```
📊 每日健康报告
2024年1月14日 星期日（昨天）

健康评分: 85分 😊 良好

昨天的数据:
- 🪑 起身次数: 8次
- 💧 喝水次数: 12次
- ⏱️ 工作时长: 7.5小时

💡 健康建议:
- ✨ 做得很好！继续保持这些健康习惯！
```

### 评分标准
- **起身次数**: 理想为每小时1次（40分）
- **喝水次数**: 理想为每小时1-2次（40分）
- **工作时长**: 理想为4-8小时（20分）

## ⚙️ 配置选项

### 启用/禁用报告
```json
{
    "movedYet.enableDailyReport": true  // 默认启用
}
```

### 相关配置
```json
{
    "movedYet.enableHistory": true  // 必须启用历史记录
}
```

## 🔧 手动查看

如果错过了自动显示，可以手动查看：

### 使用命令
```
Ctrl+Shift+P (或 Cmd+Shift+P)
→ 输入 "daily report" 或 "每日报告"
→ 选择 "Moved Yet: View Daily Health Report"
```

**注意**: 手动查看也会显示昨天的数据。

## 🧪 测试方法

### 测试自动显示
1. 确保昨天有健康活动记录
2. 确保当前时间在9点之后
3. 重启 VS Code
4. 等待5秒
5. 应该自动显示昨天的健康报告

### 测试时间条件
```bash
# 修改系统时间到9点前
# 启动 VS Code
# 结果：不显示报告

# 修改系统时间到9点后
# 启动 VS Code
# 结果：显示报告
```

### 测试数据条件
```bash
# 确保昨天没有任何健康活动
# 启动 VS Code（9点后）
# 结果：不显示报告

# 确保昨天有健康活动
# 启动 VS Code（9点后）
# 结果：显示报告
```

## 📝 技术实现

### 核心方法

#### shouldShowDailyReport()
```typescript
shouldShowDailyReport(): boolean {
    const today = this.getTodayDate();
    
    // 如果今天还没显示过报告，且当前时间在9点之后
    if (this.lastReportDate !== today && this.isAfter9AM()) {
        return true;
    }
    
    return false;
}
```

#### isAfter9AM()
```typescript
private isAfter9AM(): boolean {
    const now = new Date();
    return now.getHours() >= 9;
}
```

#### getYesterdayDate()
```typescript
private getYesterdayDate(): string {
    const yesterday = new Date();
    yesterday.setDate(yesterday.getDate() - 1);
    return yesterday.toISOString().split('T')[0];
}
```

### 数据获取

#### historyService.getYesterdayStats()
```typescript
getYesterdayStats(): DailyStats | null {
    const yesterday = new Date();
    yesterday.setDate(yesterday.getDate() - 1);
    const yesterdayDate = yesterday.toISOString().split('T')[0];
    return this.getStatsByDate(yesterdayDate);
}
```

## 🎉 优势

### 用户体验
- ✅ 不会在早上过早打扰
- ✅ 数据完整，更有参考价值
- ✅ 每天只显示一次，不重复
- ✅ 可以根据昨天调整今天

### 数据准确性
- ✅ 昨天的数据已完整记录
- ✅ 避免显示不完整的当天数据
- ✅ 评分更准确

### 实用性
- ✅ 符合工作习惯
- ✅ 时机恰当
- ✅ 便于反思和改进

## 💡 使用建议

### 最佳实践
1. **每天9点后首次启动时查看报告**
2. **根据昨天的评分和建议调整今天的计划**
3. **设定今天的健康目标**
4. **定期查看仪表盘了解长期趋势**

### 配合使用
- 查看每日报告了解昨天表现
- 查看仪表盘了解本周趋势
- 根据建议调整提醒间隔
- 持续改进健康习惯

---

**💪 每天9点，回顾昨天，规划今天，保持健康！**
