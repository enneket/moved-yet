# Moved Yet - 动了么？开发指南

## 📦 安装使用

### 方法一：命令行安装（推荐）

```bash
code --install-extension moved-yet-0.0.1.vsix
```

### 方法二：VS Code 图形界面安装

1. 打开 VS Code
2. 进入扩展面板（`Ctrl+Shift+X` / `Cmd+Shift+X`）
3. 点击右上角的 `...` 菜单
4. 选择 **"从 VSIX 安装..."**
5. 选择 `moved-yet-0.0.1.vsix` 文件
6. 等待安装完成，重新加载 VS Code

### 验证安装

```bash
# 检查插件是否已安装
code --list-extensions | grep moved-yet

# 应该显示：Immerse.moved-yet
```

---

## 🛠️ 开发环境

### 环境要求 (v0.0.1 更新)
- **Node.js**: 25.x 或更高（推荐 v25.5.0）
- **npm**: 11.x 或更高（推荐 v11.8.0）
- **pnpm**: 10.x 或更高（可选）
- **VS Code**: 1.100.0 或更高
- **TypeScript**: 5.8.3 或更高

### Node.js 版本管理
推荐使用 `fnm` 管理 Node.js 版本：

```bash
# 安装 fnm
curl -fsSL https://fnm.vercel.app/install | bash

# 安装并使用 Node.js 25.x
fnm install 25
fnm use 25
fnm default 25
```

### 开发流程

#### 1. 克隆项目
```bash
git clone <repository-url>
cd moved-yet
```

#### 2. 安装依赖
```bash
npm install
# 或
pnpm install
```

#### 3. 编译项目
```bash
# 单次编译
npm run compile

# 监听模式（开发推荐）
npm run watch
```

#### 4. 调试运行
1. 在 VS Code 中打开项目
2. 按 `F5` 启动调试
3. 会打开一个新的 VS Code 窗口（Extension Development Host）
4. 在新窗口中测试插件功能

#### 5. 代码检查
```bash
npm run lint
```

---

## 📦 编译 .vsix 包

### 🛠️ 环境准备

在开始编译之前，确保你的开发环境已准备就绪：

```bash
# 检查 Node.js 版本（推荐 16.x 或更高）
node --version

# 检查 npm 版本
npm --version

# 全局安装 vsce（VS Code Extension Manager）
npm install -g @vscode/vsce
```

### 📋 编译步骤

#### 1️⃣ 克隆项目并安装依赖
```bash
# 克隆项目
git clone <项目地址>
cd moved-yet

# 安装项目依赖
npm install
# 或使用 pnpm
pnpm install

# 编译 TypeScript
npm run compile
```

#### 2️⃣ 运行测试（可选但推荐）
```bash
# 运行所有测试
npm test

# 或者运行特定测试
npm run test:unit
```

#### 3️⃣ 打包成 .vsix 文件

**方法一：使用 vsce 命令（推荐）**
```bash
# 直接使用 vsce
vsce package

# 或使用 npx（无需全局安装）
npx @vscode/vsce package
```

**方法二：使用项目脚本**
```bash
# 使用提供的打包脚本（适用于 Node < 20）
chmod +x sh/package-vsix.sh
./sh/package-vsix.sh
```

**方法三：使用 Docker**
```bash
docker run --rm -v $(pwd):/workspace -w /workspace node:20 bash -c "
  npm install &&
  npm run compile &&
  npm install -g @vscode/vsce &&
  vsce package
"
```

#### 4️⃣ 安装编译好的扩展
```bash
# 通过命令行安装
code --install-extension moved-yet-0.0.1.vsix

# 或在 VS Code 中：
# 1. 打开扩展面板 (Ctrl+Shift+X)
# 2. 点击右上角的 "..." 菜单
# 3. 选择 "从 VSIX 安装..."
# 4. 选择编译好的 .vsix 文件
```

### 🔧 编译配置

#### package.json 关键配置
确保 `package.json` 包含正确的配置：

```json
{
  "name": "moved-yet",
  "displayName": "Moved Yet",
  "description": "动了么？—久坐和喝水强制提醒插件",
  "version": "0.0.1",
  "publisher": "Immerse",
  "engines": {
    "vscode": "^1.100.0"
  },
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.7.1",
    "typescript": "^5.8.3"
  }
}
```

#### .vscodeignore 文件
确保不必要的文件不会被打包：

```
.vscode/**
.vscode-test/**
src/**
.gitignore
.yarnrc
vsc-extension-quickstart.md
**/tsconfig.json
**/eslint.config.mjs
**/*.map
**/*.ts
**/.vscode-test.*
```

### 🚨 常见问题与解决

#### 问题 1: `vsce` 命令未找到
```bash
# 解决方案：全局安装 vsce
npm install -g @vscode/vsce

# 或使用 npx（推荐）
npx @vscode/vsce package
```

#### 问题 2: TypeScript 编译错误
```bash
# 检查 TypeScript 配置
npx tsc --noEmit

# 修复类型错误后重新编译
npm run compile
```

#### 问题 3: 依赖版本冲突
```bash
# 清理并重新安装依赖
rm -rf node_modules package-lock.json
npm install
```

#### 问题 4: 打包文件过大
检查 `.vscodeignore` 文件，确保排除了：
- `node_modules` 中的开发依赖
- 源码文件（`.ts`）
- 测试文件
- 文档和示例文件

### 📊 编译输出

成功编译后，你会得到：
- `moved-yet-0.0.1.vsix` - 可安装的扩展包
- `out/` 目录 - 编译后的 JavaScript 文件

### 🎯 自动化编译

#### 使用 GitHub Actions（推荐）
创建 `.github/workflows/build.yml`：

```yaml
name: Build Extension

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm install
      
    - name: Compile
      run: npm run compile
      
    - name: Run tests
      run: npm test
      
    - name: Package extension
      run: npx @vscode/vsce package
      
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: vsix-package
        path: '*.vsix'
```

#### 本地自动化脚本
项目已包含 `sh/package-vsix.sh` 脚本，用于手动打包（适用于 Node 版本 < 20 的环境）：

```bash
#!/bin/bash
# 使用项目提供的打包脚本
./sh/package-vsix.sh

# 脚本会自动：
# 1. 检查编译输出
# 2. 清理旧的打包文件  
# 3. 创建临时目录结构
# 4. 复制必要文件
# 5. 创建 VSIX 清单文件
# 6. 打包成 .vsix 文件
# 7. 清理临时文件
```

**脚本特点：**
- ✅ 自动检查编译状态
- ✅ 创建标准的 VSIX 包结构
- ✅ 包含完整的清单文件
- ✅ 自动清理临时文件
- ✅ 显示打包结果和安装提示

**使用方法：**
```bash
# 1. 确保已编译
npm run compile

# 2. 运行打包脚本
chmod +x sh/package-vsix.sh
./sh/package-vsix.sh

# 3. 安装生成的扩展
code --install-extension moved-yet-0.0.1.vsix
```

---

## 🏗️ 项目结构

```
moved-yet/
├── src/                          # 源代码
│   ├── extension.ts              # 插件入口
│   ├── models/
│   │   └── types.ts              # 类型定义
│   ├── services/                 # 业务逻辑层
│   │   ├── configService.ts      # 配置管理
│   │   ├── timerService.ts       # 计时器管理
│   │   ├── statusService.ts      # 状态显示
│   │   ├── historyService.ts     # 历史记录
│   │   └── progressiveReminderService.ts  # 渐进式提醒
│   ├── ui/                       # 界面层
│   │   ├── reminderUI.ts         # 提醒界面
│   │   └── dashboardUI.ts        # 仪表盘界面
│   ├── utils/
│   │   └── languages.ts          # 多语言配置
│   └── test/                     # 测试文件
├── out/                          # 编译输出
├── images/                       # 图标资源
├── docs/                         # 文档
├── package.json                  # 项目配置
├── tsconfig.json                 # TypeScript 配置
└── README.md                     # 项目说明
```

---

## 🧪 测试

### 运行测试
```bash
npm run test
```

### 测试覆盖
- 配置服务测试
- 计时器服务测试
- 状态服务测试
- 扩展主逻辑测试

### 手动测试
1. 设置短间隔（1分钟）进行快速测试
2. 测试渐进式提醒的三个级别
3. 测试历史记录功能
4. 测试仪表盘显示
5. 测试多语言切换

---

## 🔧 配置文件

### package.json 关键配置

```json
{
  "name": "moved-yet",
  "displayName": "Moved Yet",
  "description": "运动提醒—久坐和喝水强制提醒插件",
  "version": "0.0.1",
  "publisher": "Immerse",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js"
}
```

### TypeScript 配置

```json
{
  "compilerOptions": {
    "module": "Node16",
    "target": "ES2022",
    "outDir": "out",
    "lib": ["ES2022", "DOM"],
    "sourceMap": true,
    "rootDir": "src",
    "strict": true
  }
}
```

---

## 🚀 发布流程

### 1. 准备发布
- [ ] 更新版本号（`package.json`）
- [ ] 更新 `CHANGELOG.md`
- [ ] 运行测试确保无错误
- [ ] 编译项目
- [ ] 本地测试功能

### 2. 打包
```bash
npm run compile
vsce package
```

### 3. 发布到市场

#### 创建发布者账号
访问 [Visual Studio Marketplace](https://marketplace.visualstudio.com/manage)

#### 获取 Personal Access Token
1. 访问 [Azure DevOps](https://dev.azure.com/)
2. 创建 Personal Access Token
3. 权限选择：Marketplace (Manage)

#### 登录并发布
```bash
vsce login <publisher-name>
vsce publish
```

### 4. 版本管理
```bash
# 补丁版本 (0.0.1 -> 0.0.2)
vsce publish patch

# 次版本 (0.0.1 -> 0.1.0)
vsce publish minor

# 主版本 (0.0.1 -> 1.0.0)
vsce publish major
```

---

## 🐛 故障排除

### 常见问题

#### Q1: Node 版本过低
**错误**: `ReferenceError: File is not defined`
**解决**: 升级 Node.js 到 20.x 或使用 Docker 打包

#### Q2: 缺少 publisher
**错误**: `Missing publisher name`
**解决**: 在 `package.json` 中确保有 `publisher` 字段

#### Q3: 打包文件过大
**解决**: 检查 `.vscodeignore` 文件，排除不必要的文件

#### Q4: 编译错误
**解决**: 
```bash
rm -rf node_modules out
npm install
npm run compile
```

### 调试技巧

1. **查看日志**: 打开输出面板，选择 "动了么？" 频道
2. **断点调试**: 在源码中设置断点，按 F5 调试
3. **控制台输出**: 使用 `console.log` 输出调试信息
4. **重新加载**: 在调试窗口中按 `Ctrl+R` 重新加载

---

## 📚 开发资源

### 官方文档
- [VS Code 扩展开发指南](https://code.visualstudio.com/api)
- [VS Code 扩展发布指南](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)
- [TypeScript 官方文档](https://www.typescriptlang.org/docs/)

### 项目文档
- [编译指南](编译指南.md) - 详细的编译步骤和故障排除
- [功能指南](功能指南.md) - 功能使用说明
- [更新日志](CHANGELOG.md) - 版本更新记录

### 相关工具
- [vsce](https://github.com/microsoft/vscode-vsce) - VS Code 扩展打包工具
- [ESLint](https://eslint.org/) - 代码规范检查
- [Mocha](https://mochajs.org/) - 测试框架

---

## 🤝 贡献指南

### 如何贡献
1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码规范
- 遵循 ESLint 规则
- 使用 TypeScript 严格模式
- 为公共 API 添加 JSDoc 注释
- 保持代码简洁易读

### 提交规范
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

---

**祝开发愉快！** 🎉