# 发布者配置指南

## 🎯 需要配置的发布者信息

在发布扩展之前，你需要确定并配置发布者信息。

## 📋 配置步骤

### 1. 确定你的GitHub信息
- **GitHub用户名**: 你的实际GitHub用户名
- **仓库地址**: 这个项目的实际GitHub仓库地址

### 2. 选择发布者名称
你有几个选择：

#### 选项A: 使用GitHub用户名（推荐）
- **优点**: 与GitHub账户一致，容易验证
- **缺点**: 可能不够品牌化

#### 选项B: 使用品牌名称
- **优点**: 更专业，品牌化
- **缺点**: 需要在各平台单独注册

#### 选项C: 使用组织名称
- **优点**: 适合团队项目
- **缺点**: 需要GitHub组织

### 3. 更新package.json
将 `YOUR_PUBLISHER_NAME` 替换为你选择的发布者名称：

```json
{
    "publisher": "你的发布者名称"
}
```

### 4. 更新仓库信息
确保repository URL正确：

```json
{
    "repository": {
        "type": "git",
        "url": "https://github.com/你的用户名/你的仓库名"
    }
}
```

## 🔧 常见配置示例

### 示例1: 个人开发者
```json
{
    "name": "moved-yet",
    "publisher": "johnsmith",
    "repository": {
        "type": "git",
        "url": "https://github.com/johnsmith/moved-yet"
    }
}
```

### 示例2: 品牌名称
```json
{
    "name": "moved-yet",
    "publisher": "HealthyCode",
    "repository": {
        "type": "git",
        "url": "https://github.com/yourname/moved-yet"
    }
}
```

### 示例3: 组织项目
```json
{
    "name": "moved-yet",
    "publisher": "MyCompany",
    "repository": {
        "type": "git",
        "url": "https://github.com/MyCompany/moved-yet"
    }
}
```

## 📦 平台注册要求

### VS Code Marketplace
1. 需要Microsoft账户
2. 创建Azure DevOps组织
3. 发布者名称需要唯一

### Open VSX Registry
1. 使用GitHub账户登录
2. 命名空间通常与GitHub用户名匹配
3. 需要验证GitHub账户所有权

## 🔄 配置更新脚本

创建一个脚本来批量更新配置：

```bash
#!/bin/bash
# 配置发布者信息

PUBLISHER_NAME="你的发布者名称"
GITHUB_USERNAME="你的GitHub用户名"
REPO_NAME="moved-yet"

# 更新package.json
sed -i "s/YOUR_PUBLISHER_NAME/$PUBLISHER_NAME/g" package.json

# 更新README.md
sed -i "s/YOUR_PUBLISHER_NAME/$PUBLISHER_NAME/g" README.md

# 更新发布脚本
sed -i "s/YOUR_PUBLISHER_NAME/$PUBLISHER_NAME/g" sh/publish-openvsx.sh

# 更新仓库URL
sed -i "s|https://github.com/.*/.*|https://github.com/$GITHUB_USERNAME/$REPO_NAME|g" package.json

echo "配置已更新为: $PUBLISHER_NAME"
```

## ✅ 验证配置

### 检查清单
- [ ] package.json中的publisher字段已设置
- [ ] repository URL正确
- [ ] README.md中的安装命令已更新
- [ ] 发布脚本中的链接已更新
- [ ] GitHub Actions配置正确

### 测试命令
```bash
# 检查package.json配置
node -p "require('./package.json').publisher"
node -p "require('./package.json').repository.url"

# 构建测试
npm run compile
vsce package
```

## 🚀 发布准备

配置完成后，你需要：

1. **VS Code Marketplace**:
   - 注册Azure DevOps账户
   - 创建Personal Access Token
   - 设置GitHub Secret: `VSCE_PAT`

2. **Open VSX Registry**:
   - 使用GitHub账户登录
   - 创建对应的命名空间
   - 生成Access Token
   - 设置GitHub Secret: `OVSX_PAT`

## 📝 示例配置

假设你的信息是：
- GitHub用户名: `developer123`
- 发布者名称: `developer123`
- 仓库: `https://github.com/developer123/moved-yet`

那么配置应该是：

```json
{
    "name": "moved-yet",
    "publisher": "developer123",
    "repository": {
        "type": "git",
        "url": "https://github.com/developer123/moved-yet"
    }
}
```

安装命令将是：
```bash
code --install-extension developer123.moved-yet
```

---

**🎯 请根据你的实际情况修改配置，然后告诉我你的选择！**